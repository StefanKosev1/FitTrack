@page
@model FitTrack.Web.Pages.Account.LoginModel
@{
    ViewData["Title"] = "Login";
}

<section class="card">
    <p class="eyebrow">Member Access</p>
    <h1>Sign in and get back to work.</h1>
    <p class="page-lead">Use the demo login to enter the FitTrack member area. Authentication is still placeholder logic for now.</p>

    <form method="post" class="form-stack">
        <div asp-validation-summary="ModelOnly" class="validation-summary"></div>

        <label class="field">
            <span>Email</span>
            <input asp-for="Input.Email" />
            <span asp-validation-for="Input.Email" class="field-validation"></span>
        </label>

        <label class="field">
            <span>Password</span>
            <input asp-for="Input.Password" />
            <span asp-validation-for="Input.Password" class="field-validation"></span>
        </label>

        <button type="submit" class="primary-button">Login</button>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
